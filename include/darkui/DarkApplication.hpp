// DarkApplication.hpp
//
// ICS 46 Winter 2022
// Project #1: Dark at the End of the Tunnel
//
// The DarkApplication class implements the outermost layer of the
// "Dark at the End of the Tunnel" GUI.  Create an object of this
// type and call the run() function on it to launch the GUI.

#ifndef DARKAPPLICATION_HPP
#define DARKAPPLICATION_HPP



class DarkApplication
{
public:
    // run() takes the program's command-line arguments (via the argc
    // and argv parameters from main()) and launches the GUI.
    void run(int argc, char** argv);
};



#endif

